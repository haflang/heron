tak x y z = case x <= y of
  True -> z -- original returns y, with much better performance for lazy eval
  False -> let a = tak (x - 1) y z
               b = tak (y - 1) z x
               c = tak (z - 1) x y
           in b `par` c `par` tak a b c

main = tak 16 13 8
